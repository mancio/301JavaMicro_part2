spring:
  application:
    name: 301javamicro
  datasource:
    driverClassName: org.h2.Driver
    password: pass
    url: jdbc:h2:mem:testdb
    username: pass

  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

server:
  port: 8080


security:
  oauth2:
    client:
      access-token-uri: https://dev-475527.okta.com/oauth2/default/v1/token
      client-id: '0oafs6ruloU1q3amy4x6'
      client-secret: '7OgRNRZJLCOsaTAYx2Q0PJ8fZrPI_6kDg2uhuIly'
      scope: openid profile email
      user-authorization-uri: https://dev-475527.okta.com/oauth2/default/v1/authorize
    resource:
      user-info-uri: https://dev-475527.okta.com/oauth2/default/v1/userinfo

